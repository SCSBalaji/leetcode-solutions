name: Telegram Consistency Reminder

on:
  schedule:
    # Runs at 9:00 PM IST (3:30 PM UTC) every day
    - cron: '30 15 * * *'
  
  workflow_dispatch:

jobs:
  check-consistency:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Check today's commits and streak
        id: check
        run: |
          python << 'EOF'
          import subprocess
          import os
          from datetime import datetime, timedelta
          
          def get_commits_on_date(date_str):
              cmd = f'git log --oneline --after="{date_str} 00:00" --before="{date_str} 23:59" -- "1. easy/" "2. medium/" "3. hard/"'
              result = subprocess.run(cmd, shell=True, capture_output=True, text=True)
              commits = [c for c in result.stdout.strip().split('\n') if c]
              return commits
          
          def calculate_streak():
              streak = 0
              check_date = datetime.now() - timedelta(days=1)
              
              while True:
                  date_str = check_date.strftime('%Y-%m-%d')
                  commits = get_commits_on_date(date_str)
                  
                  if commits:
                      streak += 1
                      check_date -= timedelta(days=1)
                  else:
                      break
                  
                  if streak > 365:
                      break
              
              return streak
          
          def count_total_problems():
              total = 0
              for difficulty in ['1. easy', '2. medium', '3. hard']:
                  if os.path.exists(difficulty):
                      for range_folder in os.listdir(difficulty):
                          range_path = os.path.join(difficulty, range_folder)
                          if os.path.isdir(range_path):
                              total += len([d for d in os.listdir(range_path) if os.path.isdir(os.path.join(range_path, d))])
              return total
          
          # Check today's commits
          today = datetime.now().strftime('%Y-%m-%d')
          today_commits = get_commits_on_date(today)
          
          # Calculate streak
          current_streak = calculate_streak()
          total_problems = count_total_problems()
          
          if today_commits:
              current_streak += 1
          
          print(f"Today's date: {today}")
          print(f"Commits today: {len(today_commits)}")
          print(f"Current streak: {current_streak} days")
          print(f"Total problems: {total_problems}")
          
          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
              if today_commits:
                  f.write("committed=true\n")
                  f.write(f"commit_count={len(today_commits)}\n")
              else:
                  f.write("committed=false\n")
              f.write(f"streak={current_streak}\n")
              f.write(f"total={total_problems}\n")
          
          # Create message if no commits
          if not today_commits:
              if current_streak > 0:
                  streak_msg = f"⚠️ Your *{current_streak}-day streak* is at risk!"
                  urgency = "Don't break the chain!"
              else:
                  streak_msg = "🌱 Start a new streak today!"
                  urgency = "Every expert was once a beginner."
              
              message = f"""🔔 *LeetCode Daily Reminder*
          {'═'*45}
          
          Hey! You haven't solved any problem today.
          
          {streak_msg}
          
          📊 *Your Stats:*
          {'─'*45}
          • Current Streak: *{current_streak} days*
          • Total Solved: *{total_problems} problems*
          • Date: {datetime.now().strftime('%B %d, %Y')}
          {'─'*45}
          
          💡 _{urgency}_
          
          *Quick Tips:*
          • Start with an Easy problem if short on time
          • Even 1 problem a day = 365 problems a year!
          • Consistency beats intensity
          
          🚀 *Commands to get started:*
          {'─'*45}
          `python scripts/create_problem.py <num> "<title>" <diff> "<topics>"`
          `python scripts/batch_operations.py stats`
          
          Keep grinding! 💪
          """
              
              with open('reminder_telegram.txt', 'w', encoding='utf-8') as f:
                  f.write(message)
          
          EOF
      
      - name: Send reminder via Telegram
        if: steps.check.outputs.committed == 'false'
        run: |
          MESSAGE=$(cat reminder_telegram.txt)
          curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "{\"chat_id\": \"${{ secrets.TELEGRAM_CHAT_ID }}\", \"text\": \"$MESSAGE\", \"parse_mode\": \"Markdown\"}"
      
      - name: Already committed today
        if: steps.check.outputs.committed == 'true'
        run: |
          echo "✅ Great job! You already committed today!"
          echo "📊 Commits today: ${{ steps.check.outputs.commit_count }}"
          echo "🔥 Current streak: ${{ steps.check.outputs.streak }} days"
          
          # Send congratulations message
          MESSAGE="✅ *Great job!*\n\n🎉 You already solved a problem today!\n\n🔥 Current streak: *${{ steps.check.outputs.streak }} days*\n📈 Total problems: *${{ steps.check.outputs.total }}*\n\nKeep it up! 💪"
          
          curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "{\"chat_id\": \"${{ secrets.TELEGRAM_CHAT_ID }}\", \"text\": \"$MESSAGE\", \"parse_mode\": \"Markdown\"}"
            